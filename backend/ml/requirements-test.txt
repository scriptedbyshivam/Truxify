# Focused ML test dependencies. This intentionally excludes optional/heavy
# production packages such as dlib and TensorFlow because the traffic pipeline
# tests mock TensorFlow and do not exercise dlib.
pytest==9.0.3
pytest-asyncio==1.3.0

aiohttp==3.14.3
requests==2.33.0
numpy>=1.24.0
pandas==2.1.4
redis==5.0.0
sqlalchemy==2.0.23
